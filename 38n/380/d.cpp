#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    long q;
    int n = s.size();
    cin >> s >> q;
    vector<long> k(q);
    for(int i = 0;i < q;i++)
    {
        cin >> k[i];
    }
}