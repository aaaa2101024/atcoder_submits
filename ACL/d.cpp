#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int k;
    cin >> k;
    vector<int> a(n);
    vector<int> ans(n,0);
    for(int i = 0;i < n;i++)
    {
        cin >> a[i];
    }
    int ed = 0;
    for(int i = 1;i < n;i++)
    {
        
    }
    // cout << ans << endl;
}